import { chat } from "../src/chat.js";

const data = {
  messages: [
    {
      role: "system",
      content: "你是达摩院的生活助手机器人。",
    },
    {
      role: "user",
      content: "欧洲最富裕的家族",
    },
  ],
};

generation(data).then((r) => {
  console.log(r);
});
