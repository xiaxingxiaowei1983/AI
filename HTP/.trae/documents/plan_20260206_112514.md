## 核心问题分析

通过代码分析，我发现：

1. **413错误问题**：
   - 后端已经设置了请求体限制为1MB（第15-18行）
   - 但配置位置可能不够靠前，导致未全局生效
   - 550KB的图片数据仍然触发了413错误

2. **参数名问题**：
   - 后端已经支持同时接收 `imageBase64` 和 `image` 参数（第48行）
   - 前端仍然使用 `imageBase64` 参数
   - 虽然后端有兼容处理，但建议统一参数名

## 修复计划

### 步骤 1：优化后端请求体限制配置
- 修改 `backend/server.js` 文件
- 将请求体限制配置移到最顶部，确保全局生效
- 增加限制大小到5MB，确保足够容纳550KB的图片
- 添加详细的启动日志，确认配置生效

### 步骤 2：强制重启后端服务
- 终止所有占用3000端口的进程
- 重新启动后端服务
- 确认启动日志显示请求体限制配置

### 步骤 3：统一前端参数名（可选）
- 修改 `src/services/htpAnalysisService.ts` 文件
- 将请求体中的 `imageBase64` 参数改为 `image`
- 确保与后端参数名保持一致

### 步骤 4：验证修复效果
- 重启前后端服务
- 上传包含房、树、人核心元素的清晰画作
- 检查前端控制台和后端日志
- 确认413错误消失，接口返回200
- 验证返回的是火山方舟的AI分析结果，而非本地备用方案

## 预期效果

修复后，550KB的图片数据将不再触发413错误，后端接口能够正常接收和处理图片数据，返回火山方舟的AI分析结果。用户上传清晰的房树人画作后，系统将返回详细的心理分析报告，不再出现「暂未解析到画作特征」的兜底提示。