# 生产环境方案：使用火山TOS存储图片生成公网URL

## 问题分析
当前项目在开发环境中使用Base64编码的方式处理图片，这种方式对于小图片（<2MB）是可行的，但对于较大的图片（>2MB）会导致请求体过大，可能会超过火山API的请求体限制（一般是10MB）。因此，在生产环境中，建议使用火山TOS（对象存储）来存储图片，生成临时公网URL，然后将这个URL传递给智能体进行分析。

## 解决方案
实现一个后端服务，使用Node.js + Express框架，接收前端上传的图片，将其上传到火山TOS，然后生成临时公网URL并返回给前端。前端使用这个公网URL来调用智能体API。

### 实现步骤

#### 步骤1：创建后端服务

1. **初始化后端项目**：
   - 创建一个新的目录`backend`
   - 初始化Node.js项目：`npm init -y`
   - 安装必要的依赖：`npm install express multer @volcengine/tos-sdk dotenv cors`

2. **创建后端配置文件**：
   - 创建`.env`文件，配置火山TOS的密钥和其他必要的参数

3. **实现后端接口**：
   - 创建`server.js`文件，实现图片上传和URL生成的接口

#### 步骤2：修改前端代码

1. **添加图片上传函数**：
   - 在`src/services/htpAnalysisService.ts`中添加`uploadImageToTOS`函数，调用后端接口上传图片

2. **修改图片处理逻辑**：
   - 在`src/services/htpAnalysisService.ts`中修改`analyzeDrawingWithAI`函数，根据图片大小选择使用Base64还是公网URL

#### 步骤3：配置生产环境

1. **部署后端服务**：
   - 将后端服务部署到云服务器或容器中
   - 配置环境变量

2. **配置前端代理**：
   - 修改`vite.config.ts`，添加后端服务的代理规则

### 实现代码

#### 后端代码（server.js）

```javascript
const express = require('express');
const multer = require('multer');
const { TosClient } = require('@volcengine/tos-sdk');
const dotenv = require('dotenv');
const cors = require('cors');

dotenv.config();

const app = express();
app.use(cors());

// 限制文件大小为10MB
const upload = multer({ limits: { fileSize: 10 * 1024 * 1024 } });

// 初始化火山TOS客户端
const tosClient = new TosClient({
  accessKeyId: process.env.TOS_ACCESS_KEY,
  accessKeySecret: process.env.TOS_SECRET_KEY,
  region: "cn-beijing",
  endpoint: "https://tos-cn-beijing.volces.com"
});

// 后端接口：接收图片并上传到TOS，返回公网URL
app.post('/api/upload-image', upload.single('image'), async (req, res) => {
  try {
    const file = req.file;
    if (!file) return res.status(400).json({ msg: "请上传图片" });

    // 上传到TOS的参数
    const bucketName = process.env.TOS_BUCKET_NAME;
    const objectKey = `htp-images/${Date.now()}-${file.originalname}`;

    // 上传文件到TOS
    await tosClient.putObject({
      bucket: bucketName,
      key: objectKey,
      body: file.buffer,
      contentType: file.mimetype
    });

    // 生成临时公网URL（有效期1小时）
    const presignedUrl = await tosClient.presignGetObject({
      bucket: bucketName,
      key: objectKey,
      expires: 3600 // 有效期（秒）
    });

    res.json({ imageUrl: presignedUrl });
  } catch (err) {
    console.error("TOS上传失败:", err);
    res.status(500).json({ msg: "图片上传失败" });
  }
});

// 启动后端服务
const PORT = process.env.PORT || 3001;
app.listen(PORT, () => console.log(`后端服务运行在 http://localhost:${PORT}`));
```

#### 后端配置文件（.env）

```env
# 火山TOS配置
TOS_ACCESS_KEY=你的火山TOS访问密钥
TOS_SECRET_KEY=你的火山TOS秘密密钥
TOS_BUCKET_NAME=你的火山TOS桶名
TOS_REGION=cn-beijing

# 服务配置
PORT=3001
```

#### 前端代码修改（src/services/htpAnalysisService.ts）

```typescript
// 上传图片到TOS，返回公网URL
export async function uploadImageToTOS(imageData: string): Promise<string> {
  try {
    // 将Base64转换为Blob
    const response = await fetch(imageData);
    const blob = await response.blob();
    const file = new File([blob], 'htp-drawing.png', { type: 'image/png' });
    
    // 创建FormData
    const formData = new FormData();
    formData.append('image', file);
    
    // 调用后端接口
    const uploadResponse = await fetch('/api/upload-image', {
      method: 'POST',
      body: formData
    });
    
    if (!uploadResponse.ok) {
      throw new Error('图片上传失败');
    }
    
    const data = await uploadResponse.json();
    return data.imageUrl;
  } catch (error) {
    console.error('上传图片到TOS失败:', error);
    throw error;
  }
}

// 调用火山豆包API进行HTP分析
export async function analyzeDrawingWithAI(imageData: string, retryCount: number = 0): Promise<AnalysisResult> {
  const startTime = Date.now();
  
  try {
    console.log('开始调用火山豆包API...');
    console.log('API URL:', API_URL);
    console.log('Chat Model:', ARK_CHAT_MODEL);
    console.log('API Key配置状态:', !!ARK_API_KEY ? '已配置' : '未配置');
    console.log('重试次数:', retryCount);
    
    // 验证API密钥配置
    if (!ARK_API_KEY) {
      console.error('API密钥未配置，请检查环境变量VITE_ARK_API_KEY');
      console.error('当前环境变量值:', import.meta.env.VITE_ARK_API_KEY);
      throw new Error('API密钥未配置，请联系管理员');
    }
    
    // 验证API密钥格式
    if (typeof ARK_API_KEY !== 'string' || ARK_API_KEY.length < 10) {
      console.error('API密钥格式不正确，长度:', ARK_API_KEY ? ARK_API_KEY.length : 0);
      throw new Error('API密钥格式不正确，请联系管理员');
    }
    
    // 验证Chat Model配置
    if (!ARK_CHAT_MODEL) {
      console.error('Chat Model未配置，请检查环境变量VITE_ARK_CHAT_MODEL');
      throw new Error('Chat Model未配置，请联系管理员');
    }
    
    // 验证imageData参数
    if (!imageData || typeof imageData !== 'string') {
      console.error('imageData参数无效:', imageData);
      throw new Error('图像数据无效，请检查上传的图像');
    }
    
    // 验证imageData格式
    if (!imageData.startsWith('data:image/')) {
      console.error('imageData格式不正确，缺少data:image/前缀:', imageData.substring(0, 50) + '...');
      throw new Error('图像数据格式不正确，请检查上传的图像');
    }
    
    // 确定图片处理方式：根据图片大小选择使用Base64还是公网URL
    let imageUrl = imageData;
    
    // 如果图片大小超过2MB，使用TOS存储
    if (imageData.length > 2 * 1024 * 1024) {
      console.log('图片大小超过2MB，使用TOS存储');
      try {
        imageUrl = await uploadImageToTOS(imageData);
        console.log('图片上传到TOS成功，公网URL:', imageUrl);
      } catch (error) {
        console.error('上传图片到TOS失败，使用Base64作为备用方案:', error);
        // 上传失败时，使用Base64作为备用方案
      }
    }
    
    // 构建火山豆包 API 请求体（OpenAI兼容格式，支持多模态）
    const requestBody = {
      model: ARK_CHAT_MODEL,
      messages: [
        {
          role: 'system',
          content: HTP_SYSTEM_PROMPT
        },
        {
          role: 'user',
          content: [
            {
              type: 'text',
              text: '请分析以下房树人绘画作品，生成完整的心理分析报告。'
            },
            {
              type: 'image_url',
              image_url: {
                url: imageUrl // 使用Base64或公网URL
              }
            }
          ]
        }
      ],
      max_tokens: 1500,
      temperature: 0.6
    };

    // 验证请求体结构
    if (!requestBody.model || !requestBody.messages) {
      console.error('请求体结构无效:', requestBody);
      throw new Error('请求参数配置错误，请联系管理员');
    }

    console.log('请求参数准备完成，开始发送API请求...');
    console.log('请求体大小:', JSON.stringify(requestBody).length, '字符');
    console.log('消息数量:', requestBody.messages.length);

    // 添加超时设置
    const controller = new AbortController();
    const timeoutId = setTimeout(() => {
      console.error('API调用超时，60秒后自动终止');
      controller.abort();
    }, 60000); // 增加超时时间到60秒

    // 调用API
    const response = await fetch(API_URL, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${ARK_API_KEY}`,
        'Content-Type': 'application/json',
        'X-Request-Id': `htp-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
        'Accept': 'application/json'
      },
      body: JSON.stringify(requestBody),
      signal: controller.signal,
      mode: 'cors', // 添加CORS模式
      credentials: 'omit', // 不发送凭据
      cache: 'no-cache' // 禁用缓存
    });

    clearTimeout(timeoutId);

    console.log(`API响应状态码: ${response.status}`);
    console.log('API响应状态文本:', response.statusText);
    
    if (!response.ok) {
      let errorData;
      try {
        errorData = await response.json();
        console.error('API错误响应:', JSON.stringify(errorData, null, 2));
        
        // 分析常见错误类型
        if (errorData.error?.code === '401') {
          console.error('API Key 无效或未授权');
          throw new Error('API密钥无效或未授权，请联系管理员');
        } else if (errorData.error?.code === '429') {
          console.error('API 调用频率过高，已达到限流上限');
          throw new Error('API调用过于频繁，请稍后再试');
        } else if (errorData.error?.code === '500') {
          console.error('API 服务内部错误');
          throw new Error('服务器暂时无法处理请求，请稍后再试');
        } else if (errorData.error?.code === '404') {
          console.error('API端点不存在，可能是URL配置错误');
          throw new Error('API配置错误，请联系管理员');
        } else if (errorData.error?.code === '403') {
          console.error('API访问被拒绝，可能是权限问题');
          throw new Error('API访问被拒绝，请联系管理员');
        }
      } catch (jsonError) {
        console.error('解析错误响应失败:', jsonError);
        try {
          const errorText = await response.text();
          console.error('错误响应文本:', errorText);
          errorData = { error: { message: errorText } };
        } catch (textError) {
          console.error('读取错误响应文本失败:', textError);
          errorData = { error: { message: response.statusText } };
        }
      }
      throw new Error(`API调用失败: ${errorData.error?.message || errorData.message || response.statusText}`);
    }

    // 读取响应数据
    let data;
    try {
      data = await response.json();
      console.log('API成功响应:', JSON.stringify(data, null, 2));
    } catch (jsonError) {
      console.error('解析API响应失败:', jsonError);
      throw new Error('服务器返回的数据格式错误，请稍后再试');
    }
    
    // 验证响应数据结构
    if (!data) {
      console.error('API返回的数据为空');
      throw new Error('服务器返回的数据为空，请稍后再试');
    }
    
    // 验证响应数据格式
    if (data.error) {
      console.error('API返回错误:', data.error);
      const errorMessage = data.error.message || JSON.stringify(data.error);
      throw new Error(`API调用失败: ${errorMessage}`);
    }
    
    // 验证choices字段（OpenAI兼容格式）
    if (!data.choices || !Array.isArray(data.choices) || data.choices.length === 0) {
      console.error('API返回的数据缺少choices字段:', data);
      throw new Error('服务器返回的数据格式错误，缺少输出内容');
    }
    
    // 验证message字段
    if (!data.choices[0].message || !data.choices[0].message.content) {
      console.error('API返回的数据缺少message.content字段:', data);
      throw new Error('服务器返回的数据格式错误，缺少输出文本');
    }
    
    // 提取结果文本
    const resultText = data.choices[0].message.content || '';
    console.log('API返回文本长度:', resultText.length);
    
    // 验证结果文本
    if (!resultText || typeof resultText !== 'string') {
      console.error('API返回的结果文本无效:', resultText);
      throw new Error('服务器返回的分析结果无效，请稍后再试');
    }
    
    // 验证结果文本长度
    if (resultText.length < 100) {
      console.warn('API返回的结果文本过短，可能不完整:', resultText.length);
    }

    // 添加调试日志，检查返回文本格式
    console.log('🔍 API原始返回文本（开始）:', resultText.substring(0, 200) + '...');
    console.log('🔍 文本长度:', resultText.length);
    console.log('🔍 是否包含【观察】:', resultText.includes('【观察】'));
    console.log('🔍 是否包含【理解】:', resultText.includes('【理解】'));
    console.log('🔍 是否包含【成长】:', resultText.includes('【成长】'));

    console.log(`API调用完成，耗时: ${Date.now() - startTime}ms`);

    // 解析结果
    const parsedResult = parseAnalysisResult(resultText);
    
    // 验证解析结果
    console.log('解析后的API结果:', JSON.stringify(parsedResult, null, 2));
    
    // 确保返回的结果包含真实API数据，而不是默认值
    const analysisResult = {
      section_see: parsedResult.section_see || 'AI分析完成',
      section_understand: parsedResult.section_understand || 'AI分析完成',
      section_grow: parsedResult.section_grow || 'AI分析完成',
      illustrations: parsedResult.illustrations || [],
      risk_level: parsedResult.risk_level || 'low'
    };
    
    console.log('最终返回的分析结果:', JSON.stringify(analysisResult, null, 2));
    return analysisResult;

  } catch (error) {
    console.error('HTP分析失败:', error);
    console.error('错误详情:', error instanceof Error ? error.stack : error);
    
    // 分析错误类型
    if (error instanceof Error) {
      // 定义可重试的错误类型
      const isRetryableError = (
        // 超时错误
        error.name === 'AbortError' ||
        // 网络错误
        (error.message.includes('NetworkError') || error.message.includes('Failed to fetch') || error.message.includes('ERR_FAILED') || error.message.includes('fetch failed')) ||
        // 服务器临时错误
        (error.message.includes('500') || error.message.includes('502') || error.message.includes('503') || error.message.includes('504'))
      );
      
      // 如果是可重试的错误，且重试次数小于2，则自动重试
      if (isRetryableError && retryCount < 2) {
        // 实现指数退避策略，每次重试后增加等待时间
        const baseDelay = 1000; // 基础延迟时间（毫秒）
        const exponentialDelay = baseDelay * Math.pow(2, retryCount); // 指数增长的延迟时间
        const jitter = Math.random() * 500; // 添加一些随机性，避免请求风暴
        const totalDelay = exponentialDelay + jitter;
        
        console.log(`API调用失败，将在 ${Math.round(totalDelay)}ms 后重试...`);
        console.log(`错误类型: ${error.name || 'Unknown'}，错误消息: ${error.message}`);
        console.log(`重试次数: ${retryCount + 1}/2`);
        
        // 等待指定时间后重试
        await new Promise(resolve => setTimeout(resolve, totalDelay));
        return analyzeDrawingWithAI(imageData, retryCount + 1);
      }
      
      // 处理不同类型的错误
      if (error.name === 'AbortError') {
        console.error('API调用被终止（超时或用户取消）');
        throw new Error('网络连接超时，已尝试多次连接失败，请检查网络后重试');
      } else if (error.message.includes('NetworkError') || error.message.includes('Failed to fetch') || error.message.includes('ERR_FAILED') || error.message.includes('fetch failed')) {
        console.error('网络错误，可能是网络连接问题');
        throw new Error('网络连接失败，请检查网络连接后重试');
      } else if (error.message.includes('401')) {
        console.error('认证错误，API Key 可能无效');
        throw new Error('API密钥无效，请联系管理员检查API密钥配置');
      } else if (error.message.includes('429')) {
        console.error('限流错误，API 调用频率过高');
        throw new Error('API调用过于频繁，请稍后再试（建议等待30秒后重试）');
      } else if (error.message.includes('500') || error.message.includes('502') || error.message.includes('503') || error.message.includes('504')) {
        console.error('API服务内部错误或不可用');
        throw new Error('服务器暂时无法处理请求，请稍后再试（建议等待1分钟后重试）');
      } else if (error.message.includes('SSL') || error.message.includes('TLS')) {
        console.error('SSL/TLS证书错误');
        throw new Error('网络安全连接错误，请检查SSL配置或联系管理员');
      } else if (error.message.includes('404')) {
        console.error('API端点不存在');
        throw new Error('API配置错误，请联系管理员检查API地址配置');
      } else if (error.message.includes('403')) {
        console.error('API访问被拒绝，可能是权限问题');
        throw new Error('API访问被拒绝，请联系管理员检查权限设置');
      } else if (error.message.includes('400')) {
        console.error('API请求参数错误');
        throw new Error('请求参数错误，请联系管理员检查API配置');
      } else {
        console.error('其他错误:', error.message);
        throw new Error(`分析过程中发生错误: ${error.message}，请稍后重试`);
      }
    }
    
    // 其他错误
    throw new Error('分析过程中发生未知错误，请稍后重试');
  }
}
```

#### 前端代理配置（vite.config.ts）

```typescript
import path from "path"
import react from "@vitejs/plugin-react"
import { defineConfig } from "vite"
import { inspectAttr } from 'kimi-plugin-inspect-react'

// https://vite.dev/config/
export default defineConfig({
  base: './',
  envPrefix: 'VITE_', // 保留之前的配置，适配.env的VITE_前缀变量
  server: {
    port: 5173,
    open: true,
    // 核心：统一代理火山对话+文生图API，解决所有跨域
    proxy: {
      // 代理火山【对话API】：前端请求 /api/ark-chat → 转发到火山对话接口
      '/api/ark-chat': {
        target: 'https://ark.cn-beijing.volces.com/api/v3/chat/completions',
        changeOrigin: true, // 必须开！模拟跨域Origin，解决CORS核心
        secure: true, // 火山是HTTPS，设为true
        rewrite: (path) => path.replace(/^\/api\/ark-chat/, '') // 重写路径，去掉代理前缀
      },
      // 代理火山【文生图API】：前端请求 /api/ark-image → 转发到火山文生图接口
      '/api/ark-image': {
        target: 'https://ark.cn-beijing.volces.com/api/v3/images/generations',
        changeOrigin: true,
        secure: true,
        rewrite: (path) => path.replace(/^\/api\/ark-image/, '')
      },
      // 代理后端接口：前端请求 /api/upload-image → 转发到后端服务
      '/api/upload-image': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        secure: false,
        rewrite: (path) => path
      }
    }
  },
  plugins: [inspectAttr(), react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
});
```

## 验证步骤

1. **启动后端服务**：
   - 进入`backend`目录
   - 运行`npm start`启动后端服务

2. **启动前端服务**：
   - 运行`npm run dev`启动前端服务

3. **测试图片上传**：
   - 在前端页面绘制一个较大的图片（>2MB）
   - 点击"完成绘制"按钮
   - 检查控制台输出，确认图片是否成功上传到TOS并生成公网URL

4. **验证API调用**：
   - 确认智能体能够成功分析图片并返回结果
   - 检查是否不再出现"InvalidParameter"错误

## 预期结果
- 较小的图片（<2MB）使用Base64编码的方式处理
- 较大的图片（>2MB）使用火山TOS存储并生成公网URL的方式处理
- 智能体能够成功分析图片并返回结果
- 不再出现"InvalidParameter: 请求缺少必要参数或参数无效，请检查后重试"的错误

## 注意事项
- 需要在火山控制台创建TOS桶，并获取访问密钥
- 后端服务需要部署到能够访问火山TOS的环境中
- 需要配置适当的CORS策略，允许前端访问后端接口
- 生成的公网URL有有效期（默认1小时），需要在有效期内使用

这个解决方案能够有效地处理较大的图片，避免了Base64编码导致的请求体过大的问题，同时保持了开发环境的简单性。