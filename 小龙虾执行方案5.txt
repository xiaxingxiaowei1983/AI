toolName: Skill
            
status: success
          
          
---

# 🦞 OpenClaw安装技术落地方案（最终版V3）

**输出模式：严格可执行**

---

## A. 目标与边界

- **目标一句话**：在Windows电脑上成功运行OpenClaw，能够通过飞书与AI助手对话

- **本轮做（3-5）**：
  1. 验证当前配置状态（只读操作）
  2. 清理配置漂移（精确删除错误配置）
  3. 验证模型调用正常（真实调用成功）
  4. 配置飞书通道（WebSocket长连接模式）

- **本轮不做（3-5）**：
  1. 不修复Dashboard/Canvas问题（使用CLI替代）
  2. 不配置多模型架构（后续扩展）
  3. 不配置ngrok内网穿透（使用WebSocket长连接）
  4. 不部署到云服务器（本地运行）

- **约束与假设（≤10）**：
  1. 操作系统：Windows 10/11
  2. Node.js版本：v22.12.0（已安装）
  3. 使用火山引擎API Key（已有：9330bd41-xxx）
  4. 沙箱环境有文件访问限制（EPERM错误）
  5. Dashboard可能不可用（使用CLI替代）
  6. 飞书使用WebSocket长连接（无需公网IP）
  7. 每一步都可独立验证和回滚
  8. 配置文件路径：`C:\Users\L\.openclaw\openclaw.json`（已验证正确）

---

## B. 选定拆解视角

- **刀法选择**：按模块边界
- **为什么选它**：安装过程涉及配置验证、配置清理、模型调用、飞书通道等独立模块，按模块边界拆解可以确保每个模块独立可验证，失败不会影响其他模块。

---

## C. 最小闭环（MVP-0）

- **MVP-0 定义**：通过CLI命令发送消息，收到AI回复（绕过Dashboard）

- **MVP-0 的"临时项"清单**：

| 临时项 | 说明 | 移除条件 |
|--------|------|----------|
| 使用CLI测试 | 替代Dashboard | Dashboard修复后可选 |
| 不配置飞书 | 先验证模型 | 模型验证通过后配置 |
| Gateway后台运行 | 可能需要 | 确认CLI是否依赖Gateway |

---

## D. 分步清单

---

### Gate 0：验证当前状态（只读操作）

**目标**：收集证据，不做任何修改

---

#### Step 0.1｜查看当前配置

- **动作**：使用CLI命令查看当前配置
- **产出**：配置信息输出
- **验收标准**：
  - 通过：显示完整的配置信息
  - 失败：命令报错
- **验证方法**：
  ```powershell
  openclaw config get
  ```
- **回滚方式**：无需回滚（只读操作）
- **风险标记**：低

---

#### Step 0.2｜查看模型列表

- **动作**：使用CLI命令查看模型列表
- **产出**：模型列表输出
- **验收标准**：
  - 通过：显示模型列表
  - 失败：列表为空或报错
- **验证方法**：
  ```powershell
  openclaw models list
  ```
- **回滚方式**：无需回滚（只读操作）
- **风险标记**：低

---

#### Step 0.3｜查看agent命令帮助

- **动作**：查看agent命令的参数和用法
- **产出**：agent命令帮助信息
- **验收标准**：
  - 通过：显示帮助信息
  - 失败：命令不存在
- **验证方法**：
  ```powershell
  openclaw agent --help
  ```
- **回滚方式**：无需回滚（只读操作）
- **风险标记**：低

---

#### Step 0.4｜启动日志监控并发送测试消息

- **动作**：启动日志监控，同时发送测试消息
- **产出**：日志输出（包含provider/model信息）
- **验收标准**：
  - 通过：日志显示完整的请求/响应信息
  - 失败：日志显示 `Unknown model` 或其他错误
- **验证方法**：
  - 终端1：`openclaw logs --follow`
  - 终端2：`openclaw agent --message "你好"`（具体命令以Step 0.3为准）
- **回滚方式**：无需回滚（只读操作）
- **风险标记**：低
- **关键证据**：记录日志中出现的 `provider`、`model`、`Unknown model` 等关键词

---

### Gate 1：清理配置漂移（需要修改）

**目标**：根据 Gate 0 收集的证据，精确清理错误配置

**前置条件**：Gate 0 完成，已收集证据

---

#### Step 1.1｜备份当前配置

- **动作**：备份配置文件
- **产出**：配置文件备份
- **验收标准**：
  - 通过：备份文件存在
  - 失败：备份失败
- **验证方法**：
  ```powershell
  Copy-Item "C:\Users\L\.openclaw\openclaw.json" "C:\Users\L\.openclaw\openclaw.json.backup"
  ```
- **回滚方式**：删除备份文件
- **风险标记**：低

---

#### Step 1.2｜清理错误的provider配置

- **动作**：根据Gate 0的证据，删除错误的provider
- **产出**：配置文件更新
- **验收标准**：
  - 通过：配置文件中只有 `volcano` provider
  - 失败：仍有错误的provider
- **验证方法**：
  - 如果存在 `anthropic` provider：`openclaw config unset models.providers.anthropic`
  - 如果 `volcengine`/`volcengine-plan` 使用占位符API Key：`openclaw config unset models.providers.volcengine`
- **回滚方式**：恢复备份文件
- **风险标记**：高
- **Plan B**：
  - 失败信号：命令执行失败
  - 兜底方案：手动编辑配置文件

---

#### Step 1.3｜确认默认模型指向正确

- **动作**：确认默认模型指向 `volcano/ep-xxx`
- **产出**：默认模型配置正确
- **验收标准**：
  - 通过：`openclaw config get agents.defaults.model.primary` 显示 `volcano/ep-xxx`
  - 失败：显示错误的model ID
- **验证方法**：
  ```powershell
  openclaw config get agents.defaults.model.primary
  ```
- **回滚方式**：恢复备份文件
- **风险标记**：中

---

### Gate 2：最小调用验证

**目标**：证明模型能正常调用

**前置条件**：Gate 1 完成，配置已清理

---

#### Step 2.1｜确认Gateway状态

- **动作**：检查Gateway是否运行
- **产出**：Gateway状态信息
- **验收标准**：
  - 通过：Gateway正在运行
  - 失败：Gateway未运行
- **验证方法**：
  ```powershell
  openclaw health
  ```
- **回滚方式**：无需回滚
- **风险标记**：低

---

#### Step 2.2｜发送测试消息

- **动作**：发送测试消息
- **产出**：AI回复
- **验收标准**：
  - 通过：收到AI的合理回复
  - 失败：无回复或报错
- **验证方法**：
  ```powershell
  openclaw agent --message "你好，请用一句话介绍你自己"
  ```
- **回滚方式**：无需回滚
- **风险标记**：中
- **Plan B**：
  - 失败信号：`Unknown model` 或 API错误
  - 兜底方案：检查日志，确认错误原因

---

#### Step 2.3｜验证日志无错误

- **动作**：检查日志确认无 `Unknown model` 错误
- **产出**：日志确认
- **验收标准**：
  - 通过：日志无 `Unknown model` 错误
  - 失败：日志仍有错误
- **验证方法**：
  ```powershell
  openclaw logs --follow
  ```
- **回滚方式**：无需回滚
- **风险标记**：低

---

### Gate 3：飞书配置

**目标**：配置飞书通道

**前置条件**：Gate 0-2 全部通过

---

#### Step 3.1｜查看plugins命令帮助

- **动作**：查看plugins命令的参数和用法
- **产出**：plugins命令帮助信息
- **验收标准**：
  - 通过：显示帮助信息
  - 失败：命令不存在
- **验证方法**：
  ```powershell
  openclaw plugins --help
  ```
- **回滚方式**：无需回滚
- **风险标记**：低

---

#### Step 3.2｜安装飞书插件

- **动作**：安装飞书插件（以plugins --help为准）
- **产出**：飞书插件安装成功
- **验收标准**：
  - 通过：插件安装成功
  - 失败：插件安装失败
- **验证方法**：根据Step 3.1的帮助信息执行
- **回滚方式**：卸载插件
- **风险标记**：中

---

#### Step 3.3｜创建飞书应用

- **动作**：在飞书开放平台创建企业自建应用
- **产出**：获得App ID和App Secret
- **验收标准**：
  - 通过：获得App ID和App Secret
  - 失败：创建失败
- **验证方法**：在飞书开放平台操作
- **回滚方式**：删除飞书应用
- **风险标记**：低
- **前置条件**：需要飞书企业账号

---

#### Step 3.4｜配置飞书长连接

- **动作**：在飞书开放平台启用长连接模式
- **产出**：飞书应用支持WebSocket长连接
- **验收标准**：
  - 通过：飞书开放平台显示"长连接已启用"
  - 失败：配置失败
- **验证方法**：在飞书开放平台操作
- **回滚方式**：关闭长连接
- **风险标记**：中
- **关键配置**：
  1. 开启机器人能力
  2. 配置事件订阅
  3. 选择"使用长连接接收事件"

---

#### Step 3.5｜连接飞书通道

- **动作**：使用OpenClaw连接飞书
- **产出**：飞书通道连接成功
- **验收标准**：
  - 通过：`openclaw channels status` 显示飞书已连接
  - 失败：连接失败
- **验证方法**：
  ```powershell
  openclaw channels --help
  # 根据帮助信息执行连接命令
  ```
- **回滚方式**：断开飞书通道
- **风险标记**：中

---

#### Step 3.6｜飞书对话测试

- **动作**：在飞书中发送消息测试
- **产出**：飞书收到AI回复
- **验收标准**：
  - 通过：飞书收到AI的合理回复
  - 失败：无回复或报错
- **验证方法**：在飞书中给机器人发送消息
- **回滚方式**：断开飞书通道
- **风险标记**：低

---

## E. 高风险清单

| 风险项 | 触发条件 | 应对措施 |
|--------|---------|----------|
| 配置修改失败 | 命令执行失败 | 使用备份恢复 |
| 模型调用失败 | Unknown model错误 | 检查日志，确认正确的model ID |
| 沙箱文件限制 | EPERM错误 | 使用管理员权限或绕过受限操作 |
| 飞书连接失败 | App ID/Secret错误 | 重新检查配置 |
| Dashboard不可用 | UI资源缺失 | 使用CLI替代测试 |

---

## F. 最终验收（DoD）

### 用户可见体验
1. ✅ 能够通过CLI与AI对话
2. ✅ 飞书中发送消息能收到AI回复
3. ✅ AI回复内容合理，无错误信息

### 系统可观测性
1. Gateway日志无错误
2. 模型调用成功（无 `Unknown model` 错误）
3. 飞书通道状态显示已连接

### 稳定性
1. 服务连续运行 > 5分钟无崩溃
2. 连续发送3条消息均能收到回复

### 回归清单
1. Gateway能正常启动和停止
2. 模型配置正确，列表显示正常
3. CLI命令能正常执行
4. 飞书通道能正常连接和断开
5. 飞书消息能正常收发

---

## G. 复盘记录

### 已知坑点

| 坑点 | 状态 | 说明 |
|------|------|------|
| Dashboard "Not Found" | ⚠️ 已知 | 使用CLI替代 |
| Canvas "Missing file" | ⚠️ 已知 | 不影响核心功能 |
| 模型配置漂移 | 🎯 待验证 | Gate 0 收集证据 |
| 沙箱文件限制 | ⚠️ 已知 | EPERM错误 |
| Windows原生依赖 | ✅ 已解决 | 使用 --ignore-scripts |

### 下轮第一步

**执行 Gate 0：验证当前状态（只读操作）**

---

## 📋 方案总结

### 核心策略
**先验证再修改，精确清理配置，使用CLI替代Dashboard**

### 执行顺序
```
Gate 0（只读验证）→ Gate 1（配置清理）→ Gate 2（调用验证）→ Gate 3（飞书配置）
```

### 关键决策
1. **只读优先**：Gate 0 不做任何修改，只收集证据
2. **精确清理**：只删除确认错误的配置，不误删
3. **CLI替代Dashboard**：Dashboard问题不影响核心功能
4. **真实调用验证**：用真实调用成功作为验收标准

---

请您确认这份技术方案是否清晰？如果没有问题，我将开始执行 **Gate 0：验证当前状态**。🌿