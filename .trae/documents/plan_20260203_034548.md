## 修改计划

### 1. 核心功能实现

#### 1.1 积分检测逻辑
- 修改 `handleSubmit` 函数，在提交前检查用户积分
- 使用 `pointsSystem.ts` 中的 `getPointsData` 和 `reducePoints` 函数
- 设定生成决策分析所需的积分阈值（例如：100积分）

#### 1.2 积分不足提示窗口
- 创建新的提示窗口组件 `PointsInsufficientModal.tsx`
- 显示积分不足状态和"注册即送1000积分"的激励信息
- 提供"去注册"按钮，跳转到注册流程

#### 1.3 数据保存功能
- 在 `BirthInputModal.tsx` 中实现数据保存逻辑
- 使用 `localStorage` 保存用户已输入的日期和问题信息
- 确保数据保存的完整性，包括所有表单字段

#### 1.4 数据恢复功能
- 在组件加载时检查是否有保存的数据
- 如果有保存的数据，自动填充到表单中
- 实现注册流程完成后的自动数据恢复

### 2. 具体修改步骤

#### 2.1 修改 BirthInputModal.tsx
- 导入积分系统相关函数
- 修改 `handleSubmit` 函数，添加积分检测逻辑
- 实现数据保存和恢复功能
- 添加积分不足时的提示窗口显示逻辑

#### 2.2 创建 PointsInsufficientModal.tsx
- 设计积分不足提示窗口的UI
- 实现"去注册"按钮的点击事件
- 确保提示窗口的样式与项目风格一致

#### 2.3 修改 LoginModal.tsx
- 添加注册完成后的回调函数
- 确保注册流程完成后能够触发数据恢复

#### 2.4 测试流程
- 测试积分不足时的提示窗口显示
- 测试数据保存功能
- 测试注册流程完成后的数据恢复
- 测试完整的用户流程

### 3. 技术实现细节

#### 3.1 数据保存格式
```typescript
interface SavedData {
  birthInfo: BirthInfo;
  userQuestion: string;
  timestamp: number;
}
```

#### 3.2 积分检测逻辑
```typescript
const REQUIRED_POINTS = 100;
const pointsData = getPointsData();

if (pointsData.balance < REQUIRED_POINTS) {
  // 保存数据
  // 显示积分不足提示窗口
} else {
  // 扣除积分
  // 继续生成决策分析
}
```

#### 3.3 注册流程集成
- 在 `PointsInsufficientModal` 中调用 `setShowLoginModal(true)`
- 在 `LoginModal` 的 `onClose` 回调中触发数据恢复
- 确保注册完成后能够继续之前的操作流程

## 预期效果

- **积分不足时**：显示提示窗口，告知用户积分不足并激励注册
- **数据保存**：自动保存用户已输入的日期和问题信息
- **注册流程**：用户完成注册后获得1000积分
- **数据恢复**：注册完成后自动恢复之前保存的数据
- **流程继续**：用户可以无缝点击生成决策分析按钮，无需重新输入信息

## 技术要点

- 使用 `localStorage` 实现数据持久化
- 确保数据保存的完整性和安全性
- 实现流畅的用户操作流程
- 保持与现有代码风格和架构的一致性