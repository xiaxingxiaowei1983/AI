## 修改计划

### 1. 更新后端服务配置
- 修改 `server.js` 文件中的 API 调用地址，使用用户提供的应用 ID
- 调整请求参数格式，符合用户提供的 API 调用格式
- 确保 API Key 仍然从环境变量读取，保持安全性

### 2. 验证前端代码
- 确认 `chat.html` 中没有硬编码的 API Key 或应用 ID
- 确保前端代码只调用本地代理接口，不直接接触密钥

### 3. 测试服务
- 重启代理服务，验证配置是否生效
- 测试 API 调用是否成功

## 具体修改步骤

1. **修改 server.js**：
   - 更新 API 调用地址为 `https://dashscope.aliyuncs.com/api/v1/apps/e01554734bea40ef9399ea7d99040bd4/completion`
   - 调整请求参数格式，使用 `{"input": {"prompt": prompt}, "parameters": {}, "debug": {}}`
   - 保持 API Key 从环境变量读取的方式

2. **验证前端代码**：
   - 确认 `chat.html` 中没有敏感信息
   - 确保前端只调用本地代理接口

3. **重启服务**：
   - 停止当前运行的服务
   - 重新启动服务
   - 验证服务是否正常运行

4. **测试 API 调用**：
   - 使用前端测试页面发送请求
   - 验证 API 调用是否成功
   - 检查返回的响应是否正确