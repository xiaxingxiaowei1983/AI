## 集成方案

### 1. 项目结构调整
- 在 `life choice` 项目中创建认证相关组件和服务
- 复制 AWKN-LAB 的 LoginModal 组件到 life choice 项目
- 创建用户认证状态管理服务

### 2. 核心功能实现
- **用户状态检查**：
  - 检查 localStorage 中的用户信息
  - 实现无感登录逻辑
- **注册页面弹出**：
  - 当用户未登录时，自动弹出 AWKN-LAB 注册页面
  - 确保注册成功后用户可以正常使用项目功能
- **用户信息同步**：
  - 确保两个项目使用相同的 localStorage 键名
  - 实现用户信息的同步更新

### 3. 技术实现细节
- **认证服务**：
  - 创建 `src/services/authService.ts`
  - 实现用户状态检查、登录、注册等方法
- **登录模态框**：
  - 复制并适配 AWKN-LAB 的 LoginModal 组件
  - 确保样式和功能与原组件一致
- **状态管理**：
  - 使用 React Context 或简单的状态管理
  - 提供全局用户状态

### 4. 具体修改步骤

1. **创建认证服务**：
   - 创建 `src/services/authService.ts`
   - 实现用户状态检查、登录、注册方法

2. **复制并适配 LoginModal**：
   - 复制 `LoginModal.tsx` 到 `src/components/`
   - 适配样式和功能

3. **修改 App.tsx**：
   - 添加用户状态检查
   - 实现注册页面自动弹出逻辑

4. **测试集成**：
   - 测试无感登录功能
   - 测试注册页面弹出功能
   - 测试用户信息同步

## 预期效果

- **已登录用户**：直接进入项目，无需重复登录
- **未登录用户**：自动弹出 AWKN-LAB 注册页面
- **注册成功**：用户信息同步到 localStorage，可正常使用项目功能

## 技术要点

- 使用 localStorage 存储用户信息，确保跨项目共享
- 实现统一的认证流程，保持用户体验一致
- 确保两个项目的认证系统完全兼容